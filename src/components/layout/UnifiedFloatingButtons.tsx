'use client';
import Link from 'next/link';
import { FaPhoneAlt, FaWhatsapp, FaArrowRight } from 'react-icons/fa';
import { siteConfig } from '@/config/site';
import { trackCTAClick, trackPhoneClick, trackWhatsAppClick } from '@/lib/analytics';
export default function UnifiedFloatingButtons(){return <div className="fixed inset-x-0 bottom-0 z-40 border-t border-line bg-bg/95 p-3 pb-[calc(.75rem+env(safe-area-inset-bottom))] backdrop-blur md:hidden"><div className="mx-auto flex max-w-lg gap-2"><a href={`tel:${siteConfig.phone}`} onClick={trackPhoneClick} className="flex flex-1 flex-col items-center justify-center gap-1 rounded-lg border border-line py-2 text-xs font-bold text-ink"><FaPhoneAlt/>Ara</a><a href={`https://wa.me/${siteConfig.whatsapp}`} target="_blank" rel="noopener noreferrer" onClick={trackWhatsAppClick} className="flex flex-1 flex-col items-center justify-center gap-1 rounded-lg bg-whatsapp py-2 text-xs font-bold text-[#0b2815]"><FaWhatsapp/>WhatsApp</a><Link href="/#arac-sat" onClick={()=>trackCTAClick('mobile_sticky_offer')} className="flex flex-1 flex-col items-center justify-center gap-1 rounded-lg bg-accent py-2 text-xs font-bold text-ink"><FaArrowRight/>Teklif Al</Link></div></div>}
